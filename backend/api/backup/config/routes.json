{
  "routes": [
    {
      "method": "GET",
      "path": "/backups",
      "handler": "backup.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/backups/count",
      "handler": "backup.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/backups/:id",
      "handler": "backup.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/backups",
      "handler": "backup.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/backups/:id",
      "handler": "backup.update",
      "config": {
        "policies": []
      }
    }
  ]
}
